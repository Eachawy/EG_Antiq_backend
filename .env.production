# ==========================================
# ADMIN FRONTEND PRODUCTION CONFIGURATION
# ==========================================
# Environment configuration for independent admin frontend deployment
#
# Domain: admin.kemetra.org
# Service: JHipster React SPA
# ==========================================

# Application Environment
NODE_ENV=production

# Backend API URL
# The admin frontend will make API calls to this URL
BACKEND_URL=https://api.kemetra.org

# ==========================================
# DEPLOYMENT NOTES:
# ==========================================
# 1. This frontend is a static SPA that calls the API
# 2. CORS must be configured in the API to allow admin.kemetra.org
# 3. API must be accessible from the browser (not just server-to-server)
# 4. SSL certificates are managed by certbot in this deployment
# 5. NGINX serves the frontend and terminates SSL
# ==========================================

# ==========================================
# SECURITY CHECKLIST:
# ==========================================
# [ ] SSL certificates obtained for admin.kemetra.org
# [ ] API CORS configured to allow https://admin.kemetra.org
# [ ] DNS A record points admin.kemetra.org to server IP
# [ ] Firewall allows ports 80/443
# [ ] Host NGINX routes admin.kemetra.org to localhost:8445
# ==========================================
